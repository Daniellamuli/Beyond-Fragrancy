# Models

Download the three Kaggle datasets, run the data pipeline notebook,
then run the recommender notebook to regenerate these files:

- df_with_features_checkpoint.pkl
- tfidf_matrix_checkpoint.npz  
- checkpoints.pkl